/**
 * @brief Test suite for the ael toolkit, as it stands at the end of L06.
 */
#include "pihlcore/test/test.hpp"

/**
 * @brief Run the test suite.
 *
 * @return 0 on success, or -1 on failure.
 */
int main() { return pihlcore::test::runAllTests() ? 0 : -1; }
